# Federated Learning Partitions

This directory holds the partitioned hospital datasets (`Hospital_A.csv`, `Hospital_B.csv`, etc.) used by the Flower clients in our Federated Learning setup.

**Important:** These files are excluded from GitHub tracking because they exceed the 100 MB limit. Generate them locally using the data preparation scripts before starting the federated clients.
