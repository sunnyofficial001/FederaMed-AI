apiVersion: v1
kind: Secret
metadata:
  name: federamed-secrets
  namespace: federamed-ai
type: Opaque
stringData:
  # These values are placeholders. 
  # PRODUCTION: Replace with actual secrets via External Secrets Operator or CI/CD injection.
  db-password: "REPLACE_ME_VIA_CI_CD"
  redis-password: "REPLACE_ME_VIA_CI_CD"
  jwt-secret: "REPLACE_ME_VIA_CI_CD"
  encryption-key: "REPLACE_ME_VIA_CI_CD"
  aws-access-key-id: "REPLACE_ME_VIA_CI_CD"
  aws-secret-access-key: "REPLACE_ME_VIA_CI_CD"